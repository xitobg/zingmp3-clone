import React, { useEffect, useState } from "react";
import styled from "styled-components";
const Chart2 = ({ data = {} }) => {
  //   const labels = data?.times
  //     ?.filter((time, index) => +time.hour % 2 === 0)
  //     ?.map((item) => item.hour);
  //     if (data?.items) {
  //       const dataSet = [];

  //       for (let i = 0; i < 3; i++) {
  //         dataSet.push({
  //           data: data?.items[Object.keys(data?.items)[i]]
  //             ?.filter((time, index) => +time.hour % 2 === 0)
  //             ?.map((item) => item.counter),
  //         });
  //         console.log(dataSet);
  //       }
  //     }

  return <StyledChart></StyledChart>;
};

export default Chart2;
const StyledChart = styled.div``;
